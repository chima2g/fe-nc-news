how to toggle state variables in browser
Why did we have to `${article.article_id}` but on other lines it was OK to {article.article_id}

//TODO: Do we need to set state or just navigate... think we just navigate and once anything needs to know if you're logged, it'll take the value from localStorage

Users folder doesn't fit the naming convention


Todays's tasks
8. post a new comment to an existing article.
9. delete my own comments.
commentsList is now a dumb function and needs to be called correctly with just 2 arguements


Friday's tasks
Add article searching / sorting

Saturdays tasks
18. be directed to a 404 page if I go on a non-existent path/a path for a non-existent article
19. not be allowed to post an article if I have not filled in all of the form boxes.
20. not be able to log in with a non-existent username.
25. delete my own articles.
CSSS!!!!!!

